# Leave empty to use Vite dev server proxy (/api -> http://localhost:8080).
# Or set a full URL to call the API directly (ensure CORS on the backend).
VITE_API_URL=https://studentattendancebackend-production.up.railway.app/
# VITE_API_URL= http://localhost:8080/